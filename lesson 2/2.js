var action = function (a,act,b) {
    if (act==='+'){
        return a+b;
    }
    if (act==='-'){
        return a-b;
    }
    if (act==='*'){
        return a*b;
    }
    if (act==='/'){
        return a/b;
    }
}

